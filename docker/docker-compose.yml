version: '3.3'
services:
    redis_db:
      image: redis:4.0-alpine
      ports:
        - 6379:6379
      volumes:
        - ./docker-config/redis.conf:/redis.conf
      command: [ "redis-server", "/redis.conf" ]